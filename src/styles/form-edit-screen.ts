import { createStyles } from "@material-ui/core";

/**
 * Styles for form-edit screen
 */
export default createStyles({

  root: {
    margin: "0",
    maxWidth: "100%",
    padding: "0",
  },
  formEditor: {
      marginTop: "1rem",
      height: "100%",
  },
  sideBar: {
    backgroundColor: "#fff",
    minHeight: "100vw"
  }

});